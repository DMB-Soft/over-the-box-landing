---
import Smartphone from '@lucide/astro/icons/smartphone';
import Zap from '@lucide/astro/icons/zap';
import ChartBar from '@lucide/astro/icons/chart-bar';
import Gift from '@lucide/astro/icons/gift';
import Bell from '@lucide/astro/icons/bell';

const features = [
  {
    Icon: Smartphone,
    title: 'Digitális pecsétgyűjtő',
    description: 'Nincs papírkártya — az ügyfél telefonja a kártyája',
  },
  {
    Icon: Zap,
    title: 'QR kód szkennelés',
    description: 'Egy másodperc alatt pecsétel, sorban állás nélkül',
  },
  {
    Icon: ChartBar,
    title: 'Üzleti dashboard',
    description: 'Valós idejű statisztikák: ki jön vissza, mikor, mennyit',
  },
  {
    Icon: Gift,
    title: 'Rugalmas jutalmak',
    description: 'Te döntöd el mi a jutalom és hány pecsét kell hozzá',
  },
  {
    Icon: Bell,
    title: 'Kampányok',
    description: 'Push értesítések és akciók a visszatérő vendégekért',
  },
];
---

<section
  id="funkciok"
  class="py-20 animate-on-scroll"
  style="background-color: var(--color-bg);"
>
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center mb-12">
      <h2
        class="text-3xl md:text-4xl font-extrabold tracking-tight leading-tight mb-4"
        style="font-family: var(--font-heading); color: var(--color-text);"
      >
        Minden, ami a hűségprogramodhoz kell
      </h2>
      <p class="text-lg leading-relaxed max-w-xl mx-auto" style="color: var(--color-text-secondary);">
        Egyszerű eszközök, amiket a vendégeid tényleg használni fognak.
      </p>
    </div>

    <div class="max-w-2xl mx-auto flex flex-col">
      {features.map(({ Icon, title, description }, index) => (
        <div
          class="flex items-start gap-5 py-8"
          style={index < features.length - 1 ? 'border-bottom: 1px solid var(--color-border);' : ''}
        >
          <div
            class="w-14 h-14 rounded-xl flex items-center justify-center flex-shrink-0"
            style="background-color: var(--color-primary-light);"
          >
            <Icon size={24} style="color: var(--color-primary);" />
          </div>
          <div>
            <h3
              class="text-lg font-semibold mb-1"
              style="font-family: var(--font-heading); color: var(--color-text);"
            >
              {title}
            </h3>
            <p class="text-base leading-relaxed" style="color: var(--color-text-secondary);">
              {description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
